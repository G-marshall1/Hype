<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Home Page</title>
    <!-- Add the CSS stylesheets -->
</head>
<body>

<h1>Hype!</h1>

<div class="container">
    <div class="profile-box">
        <img src="{{ userProfile.picture }}" alt="Profile Picture">
        <h2>{{ userProfile.username }}</h2>
        <p>Crowns: {{ userProfile.crowns }}</p>
    </div>

    <div class="main-content">
        <div class="videos">
            {{#each videos}}
                <div class="video-item">
                    <h3>{{ title }}</h3>
                    <p>URL: {{ url }}</p>
                    <p>Description: {{ description }}</p>
                    <p>Uploaded by: {{ uploader }}</p>
                </div>
            {{/each}}
        </div>

        <div class="leaderboard">
            <h2>Leaderboard</h2>
            <ul>
                {{#each leaderboard}}
                    <li>{{ name }} - {{ crowns }} Crowns</li>
                {{/each}}
            </ul>
            <button onclick="window.location.href='/submit'">Add a submission!</button>
        </div>
    </div>
</div>

<!-- Link JavaScripts -->

</body>
</html>
